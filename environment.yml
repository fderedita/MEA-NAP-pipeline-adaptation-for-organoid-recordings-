name: organoid-mea-foundation
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy
  - scipy
  - pandas
  - matplotlib
  - seaborn
  - pyyaml
  - tqdm
  - tabulate
  - pytest
  - pyarrow
  - pip
  - pip:
      - ipykernel
      # DANDI / NWB
      - dandi
      - pynwb
      - h5py
      - remfile
      - lindi
      - fsspec
      - aiohttp
      # Ephys
      - spikeinterface[full]
      - probeinterface
      - neo
      - elephant
      - quantities
      - hdbscan
      - neurodsp
      - joblib
      - fooof
      - bycycle
      - antropy
      - powerlaw
      # Graph
      - networkx
      - bctpy
      # ML / stats
      - scikit-learn
      - statsmodels
      - umap-learn
      # Harmonization
      - neuroCombat
      - scikit-bio
      # Optional (manifold)
      - cebra
